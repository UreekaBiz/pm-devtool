import { TooltipProps } from '@chakra-ui/react';

// ********************************************************************************
// == Class =======================================================================
export const INLINE_NODE_CONTAINER_CLASS = 'inlineNodeContainer';

// == Props =======================================================================
export const tooltipProps: Partial<TooltipProps> = { hasArrow: true, fontSize: '14px', size: 'md' };
